{{#if sidebar}}
    <nav class="col-md-2 d-none d-md-block bg-light sidebar">
        <div class="sidebar-sticky">
            <ul class="nav flex-column">
                {{#each sidebar}}
                    <li class="nav-item {{{this.active}}}"><a href="{{this.target}}">{{this.name}}</a></li>
                {{/each}}
            </ul>
        </div>
    </nav>
{{/if}}

